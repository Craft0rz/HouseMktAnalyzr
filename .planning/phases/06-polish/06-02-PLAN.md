---
phase: 06-polish
plan: 02
type: execute
---

<objective>
Create user documentation and finalize the project for release.

Purpose: Enable users to install, configure, and use HouseMktAnalyzr effectively.
Output: README with installation, usage examples, and configuration guide.
</objective>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README with installation and quickstart</name>
  <files>README.md</files>
  <action>
    Create comprehensive README:

    - Project description and features
    - Installation instructions (pip, from source)
    - Quick start guide
    - Dashboard usage
    - Alert configuration
    - Configuration options
    - Development setup
  </action>
  <verify>cat README.md | head -50</verify>
  <done>README complete with installation and usage</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI entry points</name>
  <files>pyproject.toml</files>
  <action>
    Add console script entry points:

    ```toml
    [project.scripts]
    housemktanalyzr = "housemktanalyzr.dashboard.app:main"
    housemktanalyzr-alerts = "housemktanalyzr.alerts.runner:main"
    ```

    This enables running via `housemktanalyzr` command after pip install.
  </action>
  <verify>pip install -e . && housemktanalyzr-alerts --help</verify>
  <done>CLI entry points configured</done>
</task>

<task type="auto">
  <name>Task 3: Final cleanup and verification</name>
  <files>various</files>
  <action>
    Final polish:

    - Run ruff for code formatting
    - Verify all imports work
    - Test dashboard launches
    - Test alert runner
    - Ensure .gitignore covers cache files
  </action>
  <verify>ruff check src/ --fix && python -c "import housemktanalyzr"</verify>
  <done>Project ready for release</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] README is complete and accurate
- [ ] CLI entry points work
- [ ] All modules import cleanly
- [ ] No linting errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Project is installable and usable
- Phase 6 complete - v1.0 ready!
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish/06-02-SUMMARY.md`
</output>
